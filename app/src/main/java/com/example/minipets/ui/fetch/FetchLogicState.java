package com.example.minipets.ui.fetch;

public enum FetchLogicState {
    BALL_MID_THROW,     // This state signifies that the ball is mid-throw
    STANDING_BY;        // This state signifies the logic is not doing anything right now
}
